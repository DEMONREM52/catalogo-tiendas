import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tiendas - Admin - RemHub" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
